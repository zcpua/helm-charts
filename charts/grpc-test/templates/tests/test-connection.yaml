apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grpc-test.fullname" . }}-test-connection"
  labels:
    {{- include "grpc-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grpc-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
